#include<iostream>
using namespace std;
int main(){
    int x=4;
    cout<<(x=7)<<endl;
    cout<<(x==5)<<endl; // = means assigning and == means comparisons
    // arithmetic operator --> + , - , * , % ,/
    // relatonal operator --> > , < , == , >= , <= , !=
    // assignment operator --> += , -= , %= , /= , =
    // code from left to right if same hierarchy operator eg-> (p==q==r)
    return 0;
}